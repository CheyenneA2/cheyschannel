console.log("Welcome to My Website!"); // Prints a message in the browser's console

document.addEventListener("DOMContentLoaded", () => { // Runs the function after the page is fully loaded
  // alert("Hello! Thanks for visiting."); // Shows a popup alert when the page loads
});